//
//  appleMapOverlay.swift
//  OnTheWay
//
//  Created by Ellen Seidel on 11/5/15.
//  Copyright © 2015 Carli Lessard. All rights reserved.
//

import Foundation
import UIKit
import MapKit
import GoogleMaps

/*
class appleMapOverlay
{
    init() {
        //do nothing
    }
    func addRoute(directionsPath: GMSPath) {
        let length = directionsPath.count()
        var pointsInLine: [CLLocationCoordinate2D] = []
        
        
        var i: UInt = 0
        for i = 0; i < length; ++i
        {
            print(directionsPath)
            print("hit index loop")
            print(directionsPath.coordinateAtIndex(i))
            pointsInLine.append(directionsPath.coordinateAtIndex(i))
        }
        let polyline = MKPolyline(coordinates: &pointsInLine, count: Int(length))
        mapView.addOverlay(polyline)
    }
}
*/

